Ты — senior frontend engineer уровня enterprise SaaS. Проект без Tailwind, стили на чистом CSS через CSS-переменные var(--...) и классы в index.css.

Критически важно:
- Ничего не ломать. Не переименовывать существующие классы (`btn-primary`, `analytics-*`, `data-list`, etc.), если это не оговорено.
- Все новые стили добавлять только как **новый слой** `ds-*` в конец index.css.
- В TSX уменьшать inline styles: layout переводить в классы; inline оставлять только для динамики (например width прогресса).
- Не использовать style-хаки в CSS вида `[style*="..."]`. Если нужно — добавь класс в JSX/TSX.

Задача:
1) Вставить enterprise-совместимый патч `index_css_patch_v2.txt` в конец index.css.
2) Обновить 5 страниц design-секции, используя классы `ds-*` (файлы из папки tsx).
3) Провести smoke-test: night/day, мобилка, hover.

Результат:
- Визуально дороже, чище, consistent.
- Совместимо с текущей системой переменных и существующими классами.
